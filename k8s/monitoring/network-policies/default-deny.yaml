# Default deny all ingress and egress in the monitoring namespace.
# Every legitimate traffic flow must be explicitly allowed by a separate policy.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: monitoring
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
